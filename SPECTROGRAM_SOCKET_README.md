# 频谱图Socket通信使用说明

## 修改内容总结

### 1. impoltHeartMap.cpp 修改完成
- **移除功能**: 完全去掉了内部自动生成模拟频谱数据的逻辑
- **保留功能**: 纯Socket数据接收和频谱图显示
- **主要改动**:
  - ✅ 删除了 `GenerateNewTimeSlice()` 函数（旧的数据生成函数）
  - ✅ 移除了动画相关变量（`animation_time`, `is_playing`, `animation_speed`）
  - ✅ 简化了 `GenerateNewTimeSliceFromData()` 函数，去掉噪声生成
  - ✅ 无Socket数据时显示空白而不是随机数据
  - ✅ 添加资源清理函数 `CleanupSpectrogramResources()`

### 2. 数据显示行为
- **无连接时**: 显示空白频谱图（全0数据）
- **有连接时**: 显示从Socket接收的真实数据
- **数据中断时**: 立即停止更新，保持最后状态

### 3. Socket数据发送器
- 文件: `spectrogram_sender.cpp`
- 基于原始数据生成算法的独立发送程序
- 默认发送到端口5556

## 使用方法

### 1. 启动数据发送器
```bash
# 编译发送器
./build_spectrogram_sender.sh

# 运行发送器（发送到默认端口5556）
./spectrogram_sender

# 指定目标地址和端口
./spectrogram_sender 192.168.1.100 5556
```

### 2. 启动主应用程序
```bash
# 编译主程序
cd build && make

# 运行主程序
./SensorMonitor
```

### 3. 频谱图数据源配置
- 启动主应用程序后会看到频谱图窗口
- 如果没有数据发送器运行，频谱图会显示为空白
- 启动数据发送器后，频谱图会开始显示实时数据

## 数据流程

1. **发送端**: `spectrogram_sender` 生成模拟频谱数据并通过Socket发送
2. **接收端**: 主应用程序通过 `DataManager` 接收数据
3. **显示**: `impoltHeartMap.cpp` 将接收到的数据转换为频谱图显示

## 技术细节

### 数据格式
- **通道数**: 128个通道（对应128个角度）
- **每包样本数**: 8个样本
- **数据包大小**: 4096字节 (128 × 8 × 4字节)
- **数据类型**: float32
- **传输协议**: TCP Socket

### 连接配置
- **默认地址**: 127.0.0.1
- **默认端口**: 5556
- **协议**: TCP

### 性能特性
- **实时更新**: 基于22.5kHz采样率的实时数据流
- **零延迟启动**: 无数据时不生成任何模拟数据
- **内存效率**: 移除了不必要的数学运算和随机数生成

## 验证方法

1. **测试无数据状态**:
   - 只启动主应用程序
   - 频谱图应显示为空白（黑色）

2. **测试有数据状态**:
   - 启动数据发送器: `./spectrogram_sender`
   - 频谱图应开始显示彩色的移动频谱数据

3. **测试数据中断**:
   - 停止数据发送器（Ctrl+C）
   - 频谱图应停止更新，保持最后的数据状态

## 故障排除

- **频谱图为空**: 检查数据发送器是否运行
- **连接失败**: 检查端口5556是否被占用
- **数据不更新**: 检查防火墙设置和网络连接